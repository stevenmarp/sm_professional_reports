Install Python dependencies::

    pip install num2words PyPDF2 reportlab

Then drop the ``sm_professional_reports`` folder in your Odoo addons
path, restart, update the apps list and install
**Professional Report Templates** from the Apps menu.
