<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="report_purchase_order_document">
        <t t-set="style"
           t-value="env['sm.report.style'].get_style_for(doc) or env['sm.report.style'].search([('active','=',True)], limit=1)"/>
        <t t-set="company" t-value="doc.company_id or env.company"/>
        <t t-set="partner" t-value="doc.partner_id"/>
        <t t-set="currency" t-value="doc.currency_id or company.currency_id"/>
        <t t-set="lines" t-value="doc.order_line.filtered(lambda l: not l.display_type)"/>
        <t t-set="amount_untaxed" t-value="doc.amount_untaxed"/>
        <t t-set="amount_tax" t-value="doc.amount_tax"/>
        <t t-set="amount_total" t-value="doc.amount_total"/>
        <t t-set="narration" t-value="doc.note if 'note' in doc._fields else ''"/>
        <t t-set="report_kind" t-value="'purchase'"/>
        <t t-set="doc_number" t-value="doc.name"/>
        <t t-set="info_rows" t-value="[
            ('Number', doc.name or '/'),
            ('Order Date', doc.date_order and doc.date_order.strftime('%Y-%m-%d') or ''),
            ('Vendor Ref', doc.partner_ref or ''),
            ('Buyer', doc.user_id.name or ''),
            ('Currency', currency.name or ''),
        ]"/>

        <div class="page">
            <t t-call="sm_professional_reports.sm_pro_dispatcher"/>
        </div>
    </template>

    <template id="report_purchase_order">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="doc">
                <t t-call="sm_professional_reports.report_purchase_order_document" t-lang="doc.partner_id.lang or env.user.lang"/>
            </t>
        </t>
    </template>

    <record id="action_report_sm_purchase_order" model="ir.actions.report">
        <field name="name">Professional Purchase Order / RFQ</field>
        <field name="model">purchase.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">sm_professional_reports.report_purchase_order</field>
        <field name="report_file">sm_professional_reports.report_purchase_order</field>
        <field name="binding_model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_type">report</field>
        <field name="paperformat_id" ref="paperformat_sm_pro_a4"/>
        <field name="print_report_name">(object.state in ('draft','sent','to approve') and 'RFQ - %s' or 'PO - %s') % (object.name or '')</field>
    </record>
</odoo>
